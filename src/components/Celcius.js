import React, { Component } from 'react';

class Celcius extends Component {
    render() {
        return (

        <div>{this.props.celciusValue} Celcius °C</div>

        );
    }
}

export default Celcius;